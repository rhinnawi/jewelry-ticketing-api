[project]
name = "jewelry-ticketing-api"
version = "0.1.0"

[tool.poetry]
repository = "https://pypi.org"

[tool.poetry.dependencies]
fastapi = "^0.75.0"
sqlalchemy = "^1.4.0"
uvicorn = "^0.16.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.build"